name: npm_bark

permissions:
  contents: read

on:
  schedule:
    - cron: '30 1 * * *'
  workflow_dispatch:

jobs:
  send-downloads:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v7

      - uses: pnpm/action-setup@v6

      - uses: actions/setup-node@v7
        with:
          node-version: lts/*

      - name: Install dependencies
        run: pnpm add axios -w --ignore-scripts

      - name: Run TypeScript script
        env:
          BARK_KEY: ${{ secrets.BARK_KEY }}
        run: node script/npm_bark.ts
